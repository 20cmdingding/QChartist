declare sub PoissonDistributionf ' This line of code must stay here
dim PoissonDistributionfbuffer(0 to 1000) as double ' This line of code must stay here.

sub PoissonDistributionf
' Put your indicator code here
dim i as integer
for i=200 to 0 step -1
PoissonDistributionfbuffer(i)=PoissonDistribution(open(i),close(i))
next i
end sub
