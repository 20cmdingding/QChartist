declare sub betaf ' This line of code must stay here
dim betafbuffer(0 to 1000) as double ' This line of code must stay here.

sub betaf
' Put your indicator code here
dim i as integer
for i=200 to 0 step -1
betafbuffer(i)=beta(close(i),close(i+1))
next i
end sub
