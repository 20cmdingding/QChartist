declare sub LegendreCalculatef ' This line of code must stay here
dim LegendreCalculatefbuffer(0 to 1000) as double ' This line of code must stay here.

sub LegendreCalculatef
' Put your indicator code here
dim i as integer
for i=200 to 0 step -1
LegendreCalculatefbuffer(i)=LegendreCalculate(open(i),close(i))
next i
end sub
