declare sub NormalDistributionf ' This line of code must stay here
dim NormalDistributionfbuffer(0 to 1000) as double ' This line of code must stay here.

sub NormalDistributionf
' Put your indicator code here
dim i as integer
for i=200 to 0 step -1
NormalDistributionfbuffer(i)=NormalDistribution(close(i))
next i
end sub
